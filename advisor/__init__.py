"""Core library for the AstrBot plugin advisor.

The package intentionally performs no eager imports.  This keeps the offline
scanner usable before optional runtime dependencies are installed and avoids
side effects when only one helper module is needed.
"""
