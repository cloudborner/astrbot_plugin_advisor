target-version = "py311"
line-length = 100
extend-exclude = [
  ".test-data",
  "artifacts",
  "build",
  "dist",
  "source_archives",
  "source_extracted",
]

[lint]
select = ["E4", "E7", "E9", "F", "I"]
