# 测试与部署记录

更新时间：2026-08-27（Asia/Shanghai）

## 0.7.1 验收摘要

| 项目 | 结果 |
| --- | --- |
| 本地完整测试 | 230 项通过，2 项按环境设计性跳过，另有 28 个参数化子测试通过 |
| 代码与数据检查 | Python 编译、Ruff 的 E/F/I 规则（按项目约定忽略 E501）、JSON 校验、差异空白检查和敏感信息扫描通过 |
| 发布包 | 单一 `astrbot_plugin_advisor` 根目录；非法临时模块名、缓存、测试和源码扫描目录均未打包 |
| 生产兼容性 | 在服务器现有 AstrBot 4.26.7 上加载 0.7.1 成功，并保留对更高版本的兼容 |
| LLBot | OneBot 连接正常；部署只重启 AstrBot，LLBot 未重启 |
| NapCat | 账号持有人扫码后登录成功；重启可快速登录；真实适配器分页读取 220/220 条，去重与时间排序通过 |
| 真实模型 | 连续 3 次图文分析均通过证据校验，图片证据实际生效，未产生无依据的 RM 主题 |
| 图片报告 | 真实 AstrBot 渲染链路生成 1080×3263 最终报告和 1080×3852 的 50 词组压力报告，均完整且临时文件已清理 |
| 稳定性 | AstrBot、LLBot、NapCat 三容器同时运行且均无重启、无新 OOM；主机仍有约 562 MiB 可用内存和约 599 MiB 可用 Swap |
| 回滚 | 部署前版本与配置均保存在服务器 `plugin_backups` 目录 |

## 已覆盖的主要流程

- 群聊和私聊指定群的白名单、确认、草稿隔离和过期逻辑。
- LLBot、NapCat 和兼容 OneBot 历史接口的分页、去重、失败缩页与数据归一化。
- 候选词组排序、稳定编号、分页、修改、删除、合并、确认和取消。
- `/确认分词` 前模型调用次数严格为零；确认后才进行长上下文分窗分析。
- 平台占位、合并转发、引用重复、机器人消息、纯链接、时间词和刷屏污染清理。
- 自定义黑名单词汇追加、安全正则拒绝与灾难性回溯防护。
- 模型证据编号本地复核、跨群证据拒绝、无依据主题和推荐拦截。
- 图片去重、分散抽样、数量与大小限制、损坏跳过、纯文字降级和临时文件清理。
- 已安装插件按名称、目录、元数据和规范化仓库地址排除。
- 资源四档估计、市场缓存回退、固定评分、旧配置与旧统计迁移。
- 日志开关、异常信息收敛、身份与聊天正文不落分析日志。
- 词组报告与最终报告的长中文、超长插件名、50 个词组和 10 个推荐压力渲染。
- 插件安装、重载、重启和根包导入；未再出现非法 `data.plugins` 模块路径。

## 真实服务器结果

插件部署到约 2 GiB 内存的现有服务器。启动日志确认 0.7.1、1834 条资源画像、WebUI 和 OneBot 适配器均成功就绪。部署前后只重启 AstrBot，LLBot 的启动时间保持不变。

NapCat 4.18.19 使用容器内持久化 QQ 数据目录。扫码登录后重启容器，日志确认能够快速登录，未再次要求扫码。临时启用仅容器网络可访问的 OneBot HTTP 端点进行验收后已恢复原配置，并确认端点关闭。

真实历史验收直接调用插件的 `OneBotHistoryProvider`：请求上限 220 条，最终获得 220 条，调用历史接口 13 次；消息 ID 去重、时间顺序、文本及媒体组件归一化均通过。该过程只输出汇总数字，不输出群号、发送者标识或聊天正文。

真实模型验收使用合成的匿名连续对话和临时测试图片，不读取或打印模型密钥、聊天原文、账号、群号、模型正式名称或完整请求响应。每次返回的消息和图片证据都经过本地校验；三次连续运行分别耗时约 12～16 秒，临时图片均已删除。

服务器端报告验收直接调用 AstrBot 4.26.7 自带的 HTML 渲染链路。两张压力样图宽度均为 1080 像素，完整高度分别为 3263 和 3852 像素；图片可重新打开验证，未出现空文件或截断。

三容器共同运行时，AstrBot 约 514 MiB、LLBot 约 151 MiB、NapCat 约 349 MiB（限制 700 MiB），主机可用内存约 562 MiB、可用 Swap 约 599 MiB。三者均为 `OOMKilled=false`、`RestartCount=0`，最近 30 分钟内核日志没有新 OOM。

## 隐私检查

- QQ号、明文群号、昵称、聊天原文、图片内容和模型请求响应不写入分析审计。
- 未授权用户只收到“没有权限”，不回显其 QQ 号、白名单内容或内部配置。
- 插件异常日志只保留错误类型；聊天导出失败日志不写目标群号。
- 长期统计以带盐群键和有限计数保存，元数据明确标注不保存原始消息和明文群号。
- 导出聊天记录是白名单用户主动执行的独立功能；导出文件含正文，README 已提示管理员妥善保存或删除。
- 部署时未发现插件专用旧 `.log` 文件，因此没有误删 AstrBot 或其他插件共用日志。

## 仍需动作前确认的真实账号交互

NapCat 登录已由账号持有人完成。浏览器已经能够打开服务器 AstrBot 插件页面；读取配置结构和页面布局不需要发送账号内容。QQ 内实际发送 `/需求分析` 等命令属于代表用户对外发送消息，配置页保存会把当前配置重新提交给服务器，两项都应在动作发生前另行确认；未确认前不能冒充通过。

## 发布信息

构建产物位于工作区外的 `release` 目录，并同时生成 SHA-256 校验文件。部署使用 SSH Host 别名和已登录的 GitHub CLI；不使用对话中曾暴露的令牌、Cookie 或密码。
