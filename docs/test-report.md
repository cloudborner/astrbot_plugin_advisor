# 测试与部署记录

更新时间：2026-08-30（Asia/Shanghai）

## 0.9.2 分析流程简化、宽松评价与综合兜底

| 项目 | 结果 |
| --- | --- |
| Git | 发布提交 `1547f88` 已推送到 `origin/main` |
| 本地完整测试 | 273 项通过，2 项按环境设计性跳过，另有 30 个参数化子测试通过，耗时 48.46 秒 |
| 代码与发布门禁 | Ruff、差异空白、发布契约与发布包结构检查均通过；定向分析回归 93 项及 10 个子测试通过 |
| 发布包 | `astrbot_plugin_advisor-0.9.2.zip`，821863 字节；SHA-256 为 `D62B3C57A1A00ECEFEF3D9EDC16AE4284169E3C72E595AE576D3F51156FE1666` |
| 操作流程 | `/需求分析` 直接读取历史并生成词组确认页，不再要求重复发送“确认”；`/确认分词` 继续作为模型调用前的词组校验与防误触门槛 |
| 评价放宽 | 一次明确请求和共同兴趣可以形成中低优先级需求；候选功能适配下限从 0.35 调整为 0.25，最低推荐分默认及服务器当前值从 40 调整为 35，整体置信度不再直接作为功能适配硬上限 |
| 契约容错 | 非身份描述数组会清理空项、换行和超长内容；未知证据、候选越界、重复安装和明确不兼容仍严格拒绝 |
| 综合兜底 | 长上下文综合结果校验失败时，确定性合并已经通过证据校验的分段结论并标注限制，不再把整批结果丢弃；失败审计会保留真实模型阶段，不再被 `report_render` 覆盖 |
| 服务器部署 | AstrBot 4.26.7 成功加载 0.9.2 与 1834 条资源画像；0.9.1 插件目录和部署前配置保存在 `/root/astrbot/data/plugin_backups/0.9.2-deploy` |
| 连接状态 | AstrBot 正常启动，NapCat 通过 OneBot v11 重新连接；LLBot 继续保持停止 |
| 已知环境问题 | 缺少 `type` 的 Provider 坏配置仍在启动时记录一次 `KeyError`，未阻止插件、AstrBot 或 OneBot 启动，本次未修改该无关配置 |

本版本针对 11:38 的真实失败补充回归：当综合模型返回未知证据时，本地兜底只能合并此前已经通过校验的证据编号；无害描述漂移可以清理，但不能借此接受未知证据或候选。

---

## 0.9.1 私聊附加权限开关与部署记录

| 项目 | 结果 |
| --- | --- |
| Git | 发布提交 `4321afe` 已推送到 `origin/main` |
| 本地完整测试 | 269 项通过，2 项按环境设计性跳过，另有 30 个参数化子测试通过，耗时 50.75 秒 |
| 代码与发布门禁 | Ruff、差异空白检查、发布契约与发布包结构检查均通过；定向配置与权限测试 67 项及 10 个子测试通过 |
| 发布包 | `astrbot_plugin_advisor-0.9.1.zip`，820616 字节；SHA-256 为 `B96946604909F267EB531F037B75A711A7AEF8880040B5D54BC249592E3366DC` |
| 默认权限行为 | “私聊操作要求本人在群内”和“私聊导出要求本人为群管理员”均默认关闭；关闭时不调用群成员接口，私聊指定群只校验插件QQ号白名单 |
| 独立开关验证 | 成员开关开启后，分析、最近报告和重发要求本人在目标群；管理员开关开启后，私聊导出要求本人为群主或管理员，并在读取历史前完成校验 |
| 服务器部署 | AstrBot 4.26.7 成功加载 0.9.1 与 1834 条资源画像；AstrBot 自动补齐两个新配置项且均为 `false`；0.9.0 插件目录和原配置保存在 `/root/astrbot/data/plugin_backups/0.9.1-deploy` |
| 连接状态 | AstrBot 正常启动，NapCat 通过 OneBot v11 重新连接；LLBot 继续保持停止，没有被本次部署启动 |
| 已知环境问题 | AstrBot 启动仍会因一条缺少 `type` 的 Provider 坏配置记录一次 `KeyError`；该问题与插件 0.9.1 无关且未阻止插件、WebUI 或 OneBot 启动，本次未修改 Provider 配置 |

默认关闭附加校验意味着插件白名单本身就是主要授权边界，应只填写可信QQ账号。机器人仍必须已经加入目标群并具备读取群历史的能力，否则分析或导出会因没有可用历史而失败。

---

## 0.9.0 安全检查点、结构化输出与可观测性改进

| 项目 | 结果 |
| --- | --- |
| Git | 发布提交 `9e7a498` 已推送到 `origin/main` |
| 本地完整测试 | 267 项通过，2 项按环境设计性跳过，另有 30 个参数化子测试通过，耗时 71.72 秒 |
| 代码与发布门禁 | Ruff、compileall、差异空白、发布契约与发布包结构检查均通过 |
| 发布包 | `astrbot_plugin_advisor-0.9.0.zip`，820108 字节；SHA-256 为 `3FD2FF68B35F9498CC2A9EA734AA578C2D8A65C6B2BBC3751773F82886731BC7`；共 34 个文件，不含测试和 Git 数据 |
| 安全检查点 | 成功分析后保存最长 24 小时的派生报告检查点；作用域使用带盐 HMAC 群键，不保存明文群号、聊天原文、身份字段、完整提示词或模型原始输出 |
| 免 Token 查询与重发 | 新增 `/最近需求分析 [群号]` 和 `/重发需求分析 [群号]`；重发只重新渲染安全检查点，不调用模型；群聊仅允许当前群，私聊沿用成员与权限检查 |
| Provider 结构化输出 | 需求分窗、合成和候选复核优先请求原生严格 JSON Schema；只有 Provider 明确报告不支持该能力时才无 Schema 重试一次，本地字段、证据和候选范围校验继续执行 |
| 分析审计 | `analysis_audit` 升级为 schema 3，增加模型调用次数、提示/完成/总 Token、Schema 降级次数和阶段耗时；仍不记录聊天正文或完整模型响应 |
| 增量需求记忆评估 | 本版本不实现跨分析周期的长期需求记忆；当前收益不足以抵消陈旧结论和隐私风险，后续只有在具备显式开关、清除入口、证据衰减和资源门禁后再考虑 |
| 服务器部署 | AstrBot 4.26.7 成功加载 0.9.0 与 1834 条资源画像；0.8.4 插件目录和原配置保存在 `/root/astrbot/data/plugin_backups/0.9.0-deploy`；配置部署前后 SHA-256 一致；LLBot 未重启 |
| 线上兼容核验 | 服务器框架的 `Context.llm_generate` 接受并向 Provider 透传 OpenAI 兼容参数；发布代码编译和 JSON Schema 构建烟雾测试通过；OneBot v11 重连正常 |
| 部署后资源 | AstrBot 约 494 MiB、LLBot 约 416 MiB，主机 available 约 561 MiB、Swap 已用约 412 MiB；AstrBot `RestartCount=0`、`OOMKilled=false` |
| 已知环境问题 | AstrBot 启动仍会因一条缺少 `type` 的 Provider 坏配置记录一次 `KeyError`；该问题早于 0.9.0 且不影响本插件加载，本次未修改 Provider 配置 |

针对本版本新增能力，自动化测试覆盖了检查点的过期、容量边界和原子存储、明文群号隔离、无模型重发、原生 Schema 参数、显式不支持时的兼容降级、降级后的本地严格校验，以及 Token、调用次数和阶段耗时审计。部署暂存目录已经核对为 `/root/astrbot/deploy/0.9.0` 后清理；回滚备份继续保留。

---

## 0.8.4 结构修复、审计补全与部署记录

| 项目 | 结果 |
| --- | --- |
| Git | 发布提交 `4b57dd7` 已推送到 `origin/main` |
| 本地完整测试 | 261 项通过，2 项按环境设计性跳过，另有 30 个参数化子测试通过 |
| 代码与发布门禁 | Ruff、compileall、差异空白和凭据模式扫描均通过 |
| 发布包 | `astrbot_plugin_advisor-0.8.4.zip`，813343 字节；SHA-256 为 `D8C3C574FC2794BB99981C1E19BDB78AD7B9B830E23A390EC1DF092849762E78` |
| 结构修复 | JSON 语法或字段形状错误整次分析最多修复一次，只发送模型的错误输出和契约，不重发聊天上下文；未知证据、候选越界、长度超限与内容校验失败不会触发修复 |
| 审计与报告 | 总处理时限会以 `total_timeout` 和失败阶段写入 schema 2 审计；零需求报告会说明尚无证据确认的需求、证据缺口和调整具体任务词组的方法 |
| 超时配置 | 每次模型调用继续限制在 5–120 秒；超范围数值会写入请求值与实际值。部署时服务器配置已保存为 120，因此本次启动没有钳制警告 |
| 服务器部署 | AstrBot 4.26.7 成功加载 0.8.4 与 1834 条资源画像；0.8.3 插件目录和配置保存在 `/AstrBot/data/plugin_backups/0.8.4-deploy`；LLBot 未重启 |
| 匿名模型验收 | 合成图文请求约 14.22 秒完成；视觉探针、证据校验、无关主题拦截和临时图片清理通过，引用 3 条消息证据并形成 2 项需求 |
| 图片报告 | 真实 AstrBot 渲染链路生成 1080×3410 分析报告和 1080×3886 的 50 词组报告；临时图片已清理 |
| 资源状态 | 部署后 AstrBot 约 512 MiB、LLBot 约 413 MiB，主机 available 约 594 MiB、Swap 已用约 414 MiB；AstrBot RestartCount=0、OOMKilled=false，最近 30 分钟没有新 OOM |

部署包中的结构修复、`total_timeout` 阶段审计、schema 2 和零需求提示入口均已在服务器正式插件目录核验。部署后未发现插件错误；服务器暂存 ZIP、验收脚本和容器内临时文件均已清理。

---

## 0.8.3 长上下文契约漂移修复与真实验证

| 项目 | 结果 |
| --- | --- |
| Git | 修复提交 `4882655` 已推送到 `origin/main` |
| 根因 | 长上下文下模型可能把 JSON 数组字段漂移成纯字符串；旧解析器会把整批结果判为 `invalid_value`。0.8.3 只归一这一种已知形态，长度、控制字符、候选范围和证据编号校验继续保持严格 |
| 本地验证 | 253 项通过，2 项按环境设计性跳过，另有 30 个参数化子测试通过；Ruff 通过 |
| 发布包 | `astrbot_plugin_advisor-0.8.3.zip`，811099 字节；SHA-256 为 `D1F66FD4E55A5676F0FCE012584F671302A674CD192998B0EA059AE38BFE1A8D` |
| 服务器部署 | AstrBot 4.26.7 成功加载 0.8.3；0.8.2 插件目录和配置保存在 `/AstrBot/data/plugin_backups/0.8.3-deploy`；OneBot 重连正常，LLBot 未重启 |
| 真实需求分析 | 用户指定真实群的重跑以 `status=success` 完成：569 条有效消息、500 个确认词组、检测 180 张图片、选择并分析 8 张，模型阶段耗时 44.135 秒，未重试且生成了非空结果摘要；成功后日志未再出现 `invalid_value`、`failed_after_retry` 或 `total_timeout` |
| 最新报告检查 | 报告完整生成，能概括聚餐、聚会、游戏、动漫和网络梗等群聊内容，群号只在末尾小字标注；但最终置信度仅 10%，没有形成可验证需求或插件推荐。该结果防幻觉边界正确，但对插件顾问用途偏保守，不能仅凭“生成成功”判定内容质量已经理想 |
| 运行资源 | 检查时 AstrBot 约 729 MiB、LLBot 约 421 MiB，主机 available 约 336 MiB、Swap 已用约 414 MiB；AstrBot RestartCount=0、OOMKilled=false |
| 仍需改进 | 优先为结构性契约错误设计一次受限的格式修复重试；补齐总处理时限取消路径的审计记录；在低置信度零需求报告中显示证据不足原因和下一步操作。当前不建议提高每次模型调用的 120 秒上限 |

本次真实验证没有把聊天原文、模型输出或身份字段写入测试记录。审计文件只保存数量、耗时、状态和结果摘要哈希。

---

## 0.8.2 日志改进与部署记录

| 项目 | 结果 |
| --- | --- |
| Git | 发布提交 `a95905f` 已推送到 `origin/main` |
| 本地完整测试 | 251 项通过，2 项按环境设计性跳过，另有 30 个参数化子测试通过 |
| 代码与发布门禁 | Ruff、compileall、差异空白和凭据模式扫描均通过 |
| 发布包 | `astrbot_plugin_advisor-0.8.2.zip`，810874 字节；SHA-256 为 `908A0FE0E02809CE43E35AEE37B088BF075BFEB20D0ED2B3CBE1CBC55BFBAF43` |
| 服务器部署 | 旧插件目录和配置已备份到 `/AstrBot/data/plugin_backups`；AstrBot 4.26.7 成功加载 0.8.2 与 1834 条资源画像 |
| 匿名模型验收 | 合成图文请求约 10.05 秒完成；视觉探针、证据校验、无关主题拦截和临时图片清理通过。本次需求结论引用 3 条消息证据，模型未额外引用测试图片作为需求证据 |
| 图片报告 | 真实 AstrBot 渲染链路生成 1080×3410 分析报告和 1080×3886 的 50 词组报告；临时图片已清理 |
| 日志 | 需求分析阶段、状态、数量、耗时和脱敏错误原因已接入 AstrBot 统一日志；发布包中的线上代码已核验包含相应日志入口 |
| 资源状态 | 部署后 AstrBot 约 477 MiB、LLBot 约 383 MiB，主机 available 约 584 MiB、Swap 已用约 416 MiB；AstrBot RestartCount=0、OOMKilled=false，最近 30 分钟内核日志没有新 OOM |
| 未改环境项 | 没有重启 LLBot，没有修改供应商配置，也没有代表真实 QQ 用户发送命令；SSH 7897 转发告警仍属于独立环境问题 |

0.8.2 部署前的插件目录和配置分别备份为 `astrbot_plugin_advisor-directory-before-0.8.2-a95905f` 与 `astrbot_plugin_advisor_config-before-0.8.2-a95905f.json`。服务器暂存 ZIP、验收脚本和容器内临时文件均已清理。

---

## 0.8.1 审查修复与部署记录

| 项目 | 结果 |
| --- | --- |
| Git | 发布提交 `8b36f83` 已推送到 `origin/main` |
| 本地完整测试 | 250 项通过，2 项按环境设计性跳过，另有 30 个参数化子测试通过 |
| 代码与发布门禁 | Ruff、compileall、差异空白、凭据模式扫描、发布包导入与结构测试均通过 |
| 发布包 | `astrbot_plugin_advisor-0.8.1.zip`，810818 字节；SHA-256 为 `D8969CED71B98FFF391FF467B6F0DAAD0F070CEB5126D32248C799A9FB2F8969` |
| 服务器部署 | 旧插件目录和配置已直接备份到 `/AstrBot/data/plugin_backups`；AstrBot 4.26.7 成功加载 0.8.1 与 1834 条资源画像 |
| 匿名模型验收 | 合成图文请求约 11.06 秒完成；视觉探针、证据校验、无关主题拦截和临时图片清理通过。本次需求结论引用 3 条消息证据，模型未额外引用测试图片作为需求证据 |
| 图片报告 | 真实 AstrBot 渲染链路生成 1080×3410 分析报告和 1080×3886 的 50 词组报告；临时图片已清理。群号在模板中仅作为 13px 末尾标注，不进入大标题 |
| 资源状态 | 部署后 AstrBot 约 499 MiB、LLBot 约 356 MiB，主机 available 约 625 MiB、Swap 已用约 420 MiB；两容器 RestartCount=0、OOMKilled=false，最近 30 分钟内核日志没有新 OOM |
| 未改环境项 | NapCat 继续停止；没有重启 LLBot；已有 provider `KeyError: 'type'` 和 SSH 7897 转发告警仍属于独立环境问题 |

0.8.1 没有代表真实 QQ 用户发送群聊或私聊命令，也没有修改供应商配置。真实部署只重启 AstrBot；服务器暂存 ZIP、验收脚本和容器内临时文件均已清理。

---

## 0.8.0 快速交付记录

| 项目 | 结果 |
| --- | --- |
| 本地完整测试 | 238 项通过，2 项按环境设计性跳过，另有 31 个参数化子测试通过 |
| 代码与发布门禁 | Ruff 的 E4/E7/E9/F/I、差异空白、安装包导入与结构测试、敏感信息扫描均通过 |
| 发布包 | `astrbot_plugin_advisor-0.8.0.zip`；SHA-256 为 `CEBA8F9D3D5EA2852EAB6D7D52C80271EBF31146F36CCB2DC5FE2DCAEE156E7E` |
| 服务器部署 | 已备份 0.7.1 插件目录和配置；AstrBot 4.26.7 重启后成功加载 0.8.0 与 1834 条资源资料 |
| 真实模型 | 使用匿名合成对话与临时测试图片通过一次图文验收；证据校验通过、无关主题未混入、临时图片已清理 |
| 图片报告 | AstrBot 真实渲染链路生成 1080×3376 最终报告和 1080×3852 的 50 词组压力报告，文件可验证且临时文件已清理 |
| LLBot | AstrBot 的 OneBot v11 适配器保持连接；部署没有重启 LLBot |
| NapCat | 当前容器未登录且未启动 OneBot HTTP 接口，无法重复真实历史验收；为避免登录循环占用资源，测试容器已停止但未删除 |
| 浏览器验收 | 按本次交付决定取消，不计为通过 |
| 资源状态 | 停止未登录的 NapCat 后，AstrBot 约 486 MiB、LLBot 约 336 MiB，主机可用内存约 636 MiB；部署后未发现新 OOM |
| GitHub | `main` 已同步 0.8.0 发布提交；同步仅使用 GitHub CLI 的安全登录状态 |

0.8.0 的快速交付不把未执行项目写成通过。群内和私聊中的真实账号命令、浏览器配置页以及本轮 NapCat 历史分页没有重新执行；相关逻辑由自动化测试覆盖，0.7.1 的历史真实验收记录保留在下文。

---

以下为 0.7.1 发布时的历史验收记录，不代表 2026-08-29 的当前运行状态。

## 0.7.1 验收摘要

| 项目 | 结果 |
| --- | --- |
| 本地完整测试 | 230 项通过，2 项按环境设计性跳过，另有 28 个参数化子测试通过 |
| 代码与数据检查 | Python 编译、Ruff 的 E/F/I 规则（按项目约定忽略 E501）、JSON 校验、差异空白检查和敏感信息扫描通过 |
| 发布包 | 单一 `astrbot_plugin_advisor` 根目录；非法临时模块名、缓存、测试和源码扫描目录均未打包 |
| 生产兼容性 | 在服务器现有 AstrBot 4.26.7 上加载 0.7.1 成功，并保留对更高版本的兼容 |
| LLBot | OneBot 连接正常；部署只重启 AstrBot，LLBot 未重启 |
| NapCat | 账号持有人扫码后登录成功；重启可快速登录；真实适配器分页读取 220/220 条，去重与时间排序通过 |
| 真实模型 | 连续 3 次图文分析均通过证据校验，图片证据实际生效，未产生无依据的 RM 主题 |
| 图片报告 | 真实 AstrBot 渲染链路生成 1080×3263 最终报告和 1080×3852 的 50 词组压力报告，均完整且临时文件已清理 |
| 稳定性 | AstrBot、LLBot、NapCat 三容器同时运行且均无重启、无新 OOM；主机仍有约 562 MiB 可用内存和约 599 MiB 可用 Swap |
| 回滚 | 部署前版本与配置均保存在服务器 `plugin_backups` 目录 |

## 已覆盖的主要流程

- 群聊和私聊指定群的白名单、确认、草稿隔离和过期逻辑。
- LLBot、NapCat 和兼容 OneBot 历史接口的分页、去重、失败缩页与数据归一化。
- 候选词组排序、稳定编号、分页、修改、删除、合并、确认和取消。
- `/确认分词` 前模型调用次数严格为零；确认后才进行长上下文分窗分析。
- 平台占位、合并转发、引用重复、机器人消息、纯链接、时间词和刷屏污染清理。
- 自定义黑名单词汇追加、安全正则拒绝与灾难性回溯防护。
- 模型证据编号本地复核、跨群证据拒绝、无依据主题和推荐拦截。
- 图片去重、分散抽样、数量与大小限制、损坏跳过、纯文字降级和临时文件清理。
- 已安装插件按名称、目录、元数据和规范化仓库地址排除。
- 资源四档估计、市场缓存回退、固定评分、旧配置与旧统计迁移。
- 日志开关、异常信息收敛、身份与聊天正文不落分析日志。
- 词组报告与最终报告的长中文、超长插件名、50 个词组和 10 个推荐压力渲染。
- 插件安装、重载、重启和根包导入；未再出现非法 `data.plugins` 模块路径。

## 真实服务器结果

插件部署到约 2 GiB 内存的现有服务器。启动日志确认 0.7.1、1834 条资源画像、WebUI 和 OneBot 适配器均成功就绪。部署前后只重启 AstrBot，LLBot 的启动时间保持不变。

NapCat 4.18.19 使用容器内持久化 QQ 数据目录。扫码登录后重启容器，日志确认能够快速登录，未再次要求扫码。临时启用仅容器网络可访问的 OneBot HTTP 端点进行验收后已恢复原配置，并确认端点关闭。

真实历史验收直接调用插件的 `OneBotHistoryProvider`：请求上限 220 条，最终获得 220 条，调用历史接口 13 次；消息 ID 去重、时间顺序、文本及媒体组件归一化均通过。该过程只输出汇总数字，不输出群号、发送者标识或聊天正文。

真实模型验收使用合成的匿名连续对话和临时测试图片，不读取或打印模型密钥、聊天原文、账号、群号、模型正式名称或完整请求响应。每次返回的消息和图片证据都经过本地校验；三次连续运行分别耗时约 12～16 秒，临时图片均已删除。

服务器端报告验收直接调用 AstrBot 4.26.7 自带的 HTML 渲染链路。两张压力样图宽度均为 1080 像素，完整高度分别为 3263 和 3852 像素；图片可重新打开验证，未出现空文件或截断。

三容器共同运行时，AstrBot 约 514 MiB、LLBot 约 151 MiB、NapCat 约 349 MiB（限制 700 MiB），主机可用内存约 562 MiB、可用 Swap 约 599 MiB。三者均为 `OOMKilled=false`、`RestartCount=0`，最近 30 分钟内核日志没有新 OOM。

## 隐私检查

- QQ号、明文群号、昵称、聊天原文、图片内容和模型请求响应不写入分析审计。
- 未授权用户只收到“没有权限”，不回显其 QQ 号、白名单内容或内部配置。
- 插件异常日志只保留错误类型；聊天导出失败日志不写目标群号。
- 长期统计以带盐群键和有限计数保存，元数据明确标注不保存原始消息和明文群号。
- 导出聊天记录是白名单用户主动执行的独立功能；导出文件含正文，README 已提示管理员妥善保存或删除。
- 部署时未发现插件专用旧 `.log` 文件，因此没有误删 AstrBot 或其他插件共用日志。

## 仍需动作前确认的真实账号交互

NapCat 登录已由账号持有人完成。浏览器已经能够打开服务器 AstrBot 插件页面；读取配置结构和页面布局不需要发送账号内容。QQ 内实际发送 `/需求分析` 等命令属于代表用户对外发送消息，配置页保存会把当前配置重新提交给服务器，两项都应在动作发生前另行确认；未确认前不能冒充通过。

## 发布信息

构建产物位于工作区外的 `release` 目录，并同时生成 SHA-256 校验文件。部署使用 SSH Host 别名和已登录的 GitHub CLI；不使用对话中曾暴露的令牌、Cookie 或密码。
