# Changelog

## 0.10.5 - 2026-08-31

- 修复 AstrBot 与 LLBot 分容器部署时聊天记录文件无法发送的问题：导出文件改为 Base64 内容并通过 OneBot 群文件或私聊文件接口直接上传，不再向协议端传递 AstrBot 容器内路径。
- README 命令表删除“重要性”列，并把三个词组编辑命令拆分为独立表格。

## 0.10.4 - 2026-08-31

- 移除插件推荐、对比、分类和排行四个独立命令，避免与基于真实群聊证据的需求分析流程重复。
- 将插件风险并入资源画像；`/资源画像 <插件名>` 现在同时展示功能说明、适用场景和静态性能占用估计。
- 简化聊天记录导出命令为 `/导出聊天记录 [群号]`，导出格式与时间范围改由高级设置统一控制。
- 将用户文档中的全部命令合并为一张按重要性排列的命令表。

## 0.10.3 - 2026-08-31

- 对剩余语义审查候选进行低风险定向修正：中性化 28 条宣传式措辞，修复 19 个明确的配置证据错绑，并删除 23 处无信息量的模板尾句。
- 为受模板清理影响的短摘要补充具体功能信息，保持 1810 条源码语义档案全部通过严格字段、长度和证据引用校验。
- 语义质量候选由 495 条降至 451 条；剩余内容仍作为启发式复核提示，不为追求清零而改写正确描述。
- 项目元数据仓库地址迁移到 cloudborner/astrbot_plugin_advisor。

## 0.10.2 - 2026-08-31

- 逐条复核浏览器、FFmpeg 与登录凭据候选，为 37 个插件补充主流程、可选功能、自动安装和降级路径的适用范围；普通 Python 依赖不再误报为管理员前置条件。
- 语义质量审计可识别“浏览器”“登录/扫码”等自然语言条件，并显式记录源码中未使用的 Selenium requirements 例外；依赖与凭据遗漏候选由 63 条降为 0 条。
- 排除首轮样本后再次固定随机精查 40 条，修正 16 个插件的宣传式表述、平台范围、外部服务、模型与账号条件，同时保留启发式报告供后续渐进复核。

## 0.10.1 - 2026-08-31

- 对 1810 条源码语义档案固定随机抽查 40 条并回查 README、配置和源码证据，修正 8 条实质问题与 8 条样本轻微问题，包括依赖条件遗漏、条件范围写错、数据源错误、证据错绑和配置项冒充核心能力。
- 清理用户可见描述中的内部 L3/L4 资源分级，并移除一批高置信的状态、测试、API 配置和超时配置伪能力；资源风险信息仍以自然语言限制保留。
- 新增可重复执行的全量语义质量审计，报告依赖与凭据条件、绝对化要求、宣传性语言、伪能力、内部等级和可能的配置证据错绑；启发式结果只进入复核报告，不会盲目阻断索引构建。

## 0.10.0 - 2026-08-30

- 新增一键全市场源码索引流水线：读取 1834 条市场记录，计算公开 GitHub 仓库，断点下载、安全解压、成功后删除压缩包，随后静态提取功能与资源证据并自动校验。
- 完成 1810 个可访问仓库的源码扫描；24 个市场链接在固定提交和公开默认分支上均返回 404，最终索引对这些条目保留市场元数据回退，仍完整覆盖 1834 个插件。
- 能力索引不再只依赖市场简介和标签：加入 README、AstrBot 命令装饰器、配置项说明及源码资源特征，得到 README 1803 项、命令 1450 项、配置说明 1501 项、资源证据 1810 项。
- 下载流程按插件保存断点；固定提交 404 时会尝试默认分支并明确取消提交绑定。插件代码始终只作为不可信文本和 AST 读取，不安装、不导入、不执行。

## 0.9.2 - 2026-08-30

- `/需求分析` 现在直接读取历史并生成词组确认页，不再要求重复发送“确认”；模型调用前的 `/确认分词` 继续保留，避免误触发高 Token 分析并允许先修正词组。
- 放宽一次明确请求、共同兴趣和部分匹配候选的识别：候选功能适配下限由 0.35 调整为 0.25，默认最低推荐分由 40 调整为 35，整体置信度不再直接作为功能适配硬上限。
- 非身份描述数组会清理空项、换行和超长内容，不再因无害格式漂移丢弃整批分析；未知证据、候选越界、已安装重复和明确不兼容仍保持严格。
- 长上下文综合输出校验失败时，改用已经通过证据校验的分段结果进行确定性本地合并，并在报告中标注限制；修复失败审计被报告渲染阶段覆盖的问题。

## 0.9.1 - 2026-08-30

- 新增“私聊操作要求本人在群内”和“私聊导出要求本人为群管理员”两个独立开关，默认均关闭。
- 默认情况下，私聊指定群的需求分析、最近报告、重发和聊天导出只校验插件QQ号白名单；开启相应开关后才额外调用 OneBot 群成员接口校验成员或管理员身份。
- 机器人仍必须加入目标群并具备读取历史的能力；权限接口不可用时，只有已开启的附加校验会默认拒绝。

## 0.9.0 - 2026-08-30

- 新增24小时安全报告检查点：只保存去身份化的派生报告字段，群索引使用安装级 HMAC，不保存群号、账号、原始消息、完整提示词或模型原始输出。
- 新增 `/最近需求分析` 与 `/重发需求分析`；群内操作当前群，私聊指定群时重新验证成员权限，重发会重新渲染报告但不调用模型。
- 需求分析、分段综合、契约修复和候选复核优先使用 Provider 原生严格 JSON Schema；只有明确不支持 `response_format/json_schema` 时才兼容降级为原有提示词契约。
- 分析审计升级到 schema 3，新增模型调用次数、输入/输出/总 Token、Schema 降级次数和分阶段耗时，同时继续排除聊天内容和身份字段。
- 完成增量需求记忆的隐私与准确性评估；当前版本不持久化跨批次需求，继续使用完整连续窗口分析，待满足独立批次证据、衰减、重置和资源门槛后再实施。

## 0.8.4 - 2026-08-29

- JSON 语法或字段形状错误可触发一次格式修复调用；修复只接收模型的错误输出，不重发聊天上下文，且未知证据、候选越界和内容校验失败仍会直接拒绝。
- 总处理时限现在会写入带失败阶段的脱敏审计记录；超出 5–120 秒范围的模型等待配置会在 AstrBot 日志中显示请求值与实际值。
- 零需求报告会区分“尚无证据确认的需求”和“候选插件不合格”，并提示如何调整具体任务词组后重试。

## 0.8.3 - 2026-08-29

- 修复长上下文下模型把数组字段（如 uncertainties、capabilities）输出成纯字符串时整批分析被判定失败的问题；现在会把该已知漂移形态归一为单元素数组，证据编号等关键校验保持严格不变。
- 三处模型提示词的输出契约显式要求所有列表字段必须输出 JSON 数组，即使只有一条或为空。

## 0.8.2 - 2026-08-29

- 精简 README 的隐私与日志说明，并移除“常见问题”章节。
- 需求分析的阶段、状态、数量、耗时和脱敏错误原因会写入 AstrBot 统一日志；图文降级会同时记录首次失败原因，不记录模型输出或聊天内容。

## 0.8.1 - 2026-08-29

- 新增独立的插件功能语义索引，使用现有市场说明、标签和确定性插件分类生成简短摘要与规范化能力词，无需重新全量下载或执行插件源码。
- 群需求候选召回与模型复核会读取版本匹配的语义资料；语义索引只扩大候选范围，不能替代聊天证据、选择候选外插件或修改本地固定评分。
- 群号不再出现在词组或分析报告大标题中，只在报告末尾以小字标注；同时说明图片报告可能经过 AstrBot 配置的远程渲染端点。
- 为实时历史和分析草稿增加跨群全局消息、字符与图片上限；同一操作者的新草稿会替换旧草稿，避免私聊确认到错误目标群。
- 为完整的模型分析与候选复核增加总处理时限，并把分词、长上下文切窗移出事件循环；资源索引先读取轻量元数据，只完整解析最终可用候选。
- 移除内置插件分类对群需求分词的固定种子影响，修正 AstrBot 未配置 `modalities` 时的图片能力判断。
- 发布包排除源码扫描中间产物、签名维护工具和开发 Schema；运行时 GitHub 客户端不再隐式读取进程中的 `GITHUB_TOKEN`，网络请求使用无过期版本号的统一 User-Agent。
- 新增仓库级 Ruff 配置，统一执行导入、语法与未定义名称检查。

## 0.8.0 - 2026-08-29

- 重写需求分析、长上下文综合和候选插件复核提示词；图片证据与实际附带顺序绑定，纯文字降级不再允许推测未发送图片。
- 新增群画像、排斥能力、需求、搜索词和候选插件的本地证据校验；模型复核失败时不回退到未经复核的推荐。
- 旧 `/插件推荐` 不再绕过分词确认，插件排行也不再调用旧群主题模型。
- 私聊分析指定群会验证机器人与操作者的群成员身份；私聊导出仅允许目标群管理员或群主。
- 用户提示和普通运行日志不再拼接模型或历史接口的原始异常文本。
- 图片发送前读取 AstrBot 模型能力声明；纯文本模型不会收到图片。本地路径限制在可信缓存目录，远程连接校验实际对端地址，取消或超时会清理临时文件。
- 同一用户的多个目标群草稿按用户、平台和群分别保存，确认或取消其中一个不会覆盖另一个。
- 推荐报告拆分为“最值得安装、次要推荐、其他可选”，并显示资源评估依据、置信度和临时静态估计风险。
- 删除 RoboMaster、洛克王国和人格陪伴等固定领域种子；领域只能从本次群聊、已确认词组或成功分析的图片证据中产生。
- 删除旧的聚合主题模型及其缓存、评分入口；短英文领域别名改为按完整词边界匹配，避免从普通单词内部误生成 RM 等词组。

## 0.7.1

- 修复 NapCat 4.18 将短消息 ID 当作可递减序号的问题；现在会使用原始锚点并按时间向前分页，同时兼容 LLBot 的数字序号分页。
- 完善统一历史消息结构，补充消息类型、匿名回复关系、机器人标记和来源平台；发送给模型的引用只使用本次草稿内的匿名证据编号。
- 新增脱敏的 NapCat 真实环境验收脚本，验证分页、去重、时间排序和组件归一化，不输出群号、发送者或聊天正文。
- 在约 2 GiB 的现有服务器上完成 NapCat 持久登录、重启快速登录和 220 条真实历史读取验收。

## 0.7.0

- `/需求分析` 改为“读取历史—确认词组—模型分析—插件评分”四阶段流程；确认词组前绝不调用模型。
- 新增 `/显示全部分词`、`/修改分词`、`/删除分词`、`/确认分词` 和 `/取消分析`；词组编号在草稿内保持稳定，隐藏词组仍参加分析。
- 中文词组提取不再生成连续滑动碎片；清除平台组件、合并转发、卡片、CQ 码和长标识等噪声，并支持高级黑名单词汇与安全正则。
- 模型改为读取去身份化的连续聊天上下文并引用消息或图片证据；长记录按连续窗口完整覆盖后再综合，拒绝不存在的证据编号和无证据主题。
- 图片分析默认开启并限量、去重、分散采样；无效图片局部跳过，图文调用不可用时只重试一次文字分析。完全不下载、抽帧或识别视频。
- 推荐前同时使用运行时插件清单、名称、目录和仓库身份排除已安装插件；同仓库不同名称或分支也会识别，现有能力已覆盖时不再凑数推荐。
- 新增独立的词组确认图片和分层需求分析图片；推荐分、资源档级、选择原因与风险位于同一卡片，删除“聚合需求计数”和内部 JSON。
- 配置页精简为四项普通设置，其余必要选项集中到高级设置；市场回退、资源索引、签名、评分权重和“是否使用模型”等内部策略不再暴露。
- 分析数据升级到第 4 版；迁移时备份旧文件并清理旧词频、共现碎片、主题推断和历史去重状态。
- 新增有界模型调用审计，仅记录状态、数量、耗时和结果摘要哈希，不记录聊天原文、QQ号、群号或凭据。
- 兼容服务器现有 AstrBot 4.26.7，并继续支持更高版本。

## 0.6.0

- 新增 LLBot、NapCat 和兼容 OneBot `get_group_msg_history` 接口的历史消息适配器；需求分析前自动分页补录最近消息。
- 历史补录排除机器人自身消息，只写入原有去身份化聚合统计；使用加盐消息指纹跨重启去重，不持久化补录原文、QQ号或明文群号。
- 新增 `/导出聊天记录` 命令，可在群聊导出当前群，或在私聊指定群号；支持 JSON、JSONL、TXT，媒体仅保存引用信息，不下载原文件。
- 历史请求串行执行，单页失败会缩小批量重试，并限制单次消息数、响应大小和本地去重状态大小，降低低配置服务器的峰值压力。
- 非 OneBot 平台会自动降级为只分析插件启用后收到的消息，不影响原有实时聚合统计。

## 0.5.2

- 修复私聊发送 `/需求分析 群号 确认` 时确认词被 AstrBot 参数解析器丢弃、命令反复要求确认的问题。

## 0.5.1

- 精简配置页说明，移除常用设置和高级设置分组中的冗余提示。

## 0.5.0

- 新增“QQ号白名单”，只有配置中的QQ号可以调用插件顾问命令；空白名单默认拒绝所有命令。
- 新增私聊指定群分析：白名单用户可发送 `/需求分析 群号`，确认后使用 `/需求分析 群号 确认` 读取该群的去身份化聚合数据并生成报告。
- 白名单只控制主动命令，不限制被动群统计，避免只统计少数白名单成员而扭曲群需求。
- 私聊输入的群号不持久化；机器人未加入或尚无统计数据时给出明确提示。

## 0.4.0

- 重写用户说明，突出功能、常用命令、资源等级和隐私说明；开发与扫描细节不再堆在首页。
- `/群需求分析` 更名为 `/需求分析`；首次调用只询问，发送 `/需求分析 确认` 后才会跳过缓存并重新分析。
- 体检、推荐、风险、对比、需求分析、分类和排行默认渲染为图片，渲染失败自动回退文字。
- 高级设置改为可见，新增图片报告和运行日志开关；运行日志默认开启。
- 删除 `/刷新插件数据` 命令和元数据中的平台枚举。

## 0.3.1

- 新增 1815 个插件的本地源码只读静态资源画像，并生成覆盖全部 1834 个市场条目的运行时索引；没有源码的 19 项沿用已验证元数据画像。
- 逐插件 LLM 资源补判改为默认关闭；模型文件名、依赖名、User-Agent 和日志关键词不再直接抬高资源等级。
- 细化本地模型、浏览器启动、FFmpeg、下载器、完整媒体读取、有界缓存、后台循环和线程 CPU 任务的静态规则。
- 普通配置页只显示 4 个常用选项；7 个低频高级值保留在配置文件中并默认隐藏，默认配置即可工作。
- 移除市场地址、GitHub 回退、签名索引更新、自定义主题/关键词/正则、评分权重和安全限额等用户入口，旧配置中的这些值会被忽略并使用受控内置策略。
- 群聊需求统计改为默认开启，仍可由用户一键关闭；不保存原文、账号或明文群号，也不读取平台昵称字段。聊天中的称呼可能成为达到阈值的聚合词项。
- 模型输入升级为有界的去身份化结构化摘要：去重后的样本量、内容构成、高频词、共现关系、14 日趋势、意图与命令计数，最终 JSON 不超过 20 KiB。
- 相同消息每日最多计入 3 次内容量，语义特征只计第一份，防止刷屏制造高频主题；只有达到跨消息隐私阈值的特征才会持久化或交给模型。
- 模型可发现内置主题之外的新需求，但必须引用摘要中的特征 ID；模型不能选择插件 ID、修改评分或触发安装，新增需求由本地规则匹配市场元数据并限制影响强度。
- 管理员命令 `/更新推荐索引` 更名为 `/刷新插件数据`，只刷新官方市场缓存；资源画像随插件版本发布。

## 0.2.0

- 将群聊需求匹配固定为 30 分、市场使用固定为 20 分；0.7.0 起市场分使用下载 12 分、Stars 8 分。
- 新增有界高频词统计、安全正则、RoboMaster/洛克王国/人格陪伴等版本化主题分类和可解释插件匹配。
- 新增 `/插件分类`、完整分页 `/插件排行`，并让推荐/对比命令使用群主题匹配强度。
- WebUI 配置扩展为 8 个详细分组，兼容 0.1 版扁平配置，并统一执行 URL、数值、规则数量和正则安全校验。
- 新增只接收匿名聚合特征的群需求模型解释；严格限制主题、字段和置信度，不发送原始聊天。
- GitHub 全市场扫描改为“Commit + Tree 强制阶段、SBOM 可选阶段、低额度 checkpoint”，部分扫描永不冒充完整索引。
- 新增只读 AstrBot SQLite 历史对话聚合分析脚本，报告不含原文、用户/群/会话标识。
- 强化签名索引的语义、覆盖率、时间、版本和防重放校验，并修复已安装插件大小写/名称映射。
- 修复长时间运行主机上手动注入市场记录后缓存时间戳仍为零、导致排行命令意外重新拉取实时市场的问题，并加入主机运行超过缓存期的回归测试。

## 0.1.0

- 首个可测试版本。
- 接入 AstrBot 官方市场、GitHub Tree/SBOM 和签名离线资源索引。
- 实现固定 100 分推荐规则、匿名群需求、资源冲突提示和结构化模型回退。
- 提供中文体检、推荐、风险、画像、对比、需求分析和索引更新命令。
- 内置 1834 条完整市场画像，GitHub Tree 证据覆盖率 98.96%，并验证市场版本与 GitHub Commits API 提交 OID 绑定。
- 修正 Tree SHA 与 Commit SHA 的语义混用；提交绑定改用不含文件 patch 的 List Commits 元数据接口，回退缓存按插件、版本和仓库隔离。
- 磁盘、网络、空闲 CPU 与低置信度会在固定 15 分资源适配项内保守扣分。
- 为市场下载和 GitHub API 增加网络瞬断重试、指数退避及完整扫描前额度预检。
